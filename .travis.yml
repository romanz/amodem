sudo: false
language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

install:
  - pip install .
  - pip install coveralls
  - pip install pep8

script:
  - pep8 amodem/ scripts/ tests/ amodem-cli
  - cd tests
  - coverage run --source=amodem -m py.test

after_success:
  - coveralls
